# Random Video Chat

Simple random video chat demo using WebRTC and Socket.IO for signaling.

Quick start:

1. Install dependencies

```bash
npm install
```

2. Start server

```bash
npm start
```

3. Open two browser tabs at http://localhost:3000 and click "Start Camera" then "Find Random".
